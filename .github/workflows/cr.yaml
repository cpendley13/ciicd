name: build docker image
on:
  push:
    branches: [main]    


jobs:
  build_image:
    name: build_image
    runs-on: ubuntu-latest


    steps:
      - name: check out the repo
        uses: actions/checkout@v3 